@model MainOps.Models.BoQHeadLine

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>BoQHeadLine</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ProjectId" class="control-label"></label>
                <select asp-for="ProjectId" class="form-control" asp-items="ViewBag.ProjectId"></select>
            </div>
            <div class="form-group">
                <label asp-for="BoQnum" class="control-label"></label>
                <input asp-for="BoQnum" class="form-control" />
                <span asp-validation-for="BoQnum" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="HeadLine" class="control-label"></label>
                <input asp-for="HeadLine" class="form-control" />
                <span asp-validation-for="HeadLine" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Type" class="control-label"></label>
                <select asp-for="Type" class="form-control">
                    <option value="Mobilization">@SharedLocalizer.GetLocalizedHtmlString("Mobilization")</option>
                    <option value="Installation">@SharedLocalizer.GetLocalizedHtmlString("Installation")</option>
                    <option value="Rental">@SharedLocalizer.GetLocalizedHtmlString("Rental")</option>
                    <option value="Hours">@SharedLocalizer.GetLocalizedHtmlString("Hours")</option>
                    <option value="ExtraWork">@SharedLocalizer.GetLocalizedHtmlString("Extra Work")</option>
                    <option value="Other">@SharedLocalizer.GetLocalizedHtmlString("Other")</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
</div>

