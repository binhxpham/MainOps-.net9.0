@model  MainOps.Models.WTPClasses.MixedClasses.MediaEfficiency


@{
    ViewData["Title"] = SharedLocalizer.GetLocalizedHtmlString("Delete");
}

<h2>@SharedLocalizer.GetLocalizedHtmlString("Delete")</h2>

<h3>@SharedLocalizer.GetLocalizedHtmlString("Are you sure you want to delete this filtermaterial/contamination combination?")</h3>
<div>
    <h4>@SharedLocalizer.GetLocalizedHtmlString("Delete Mediaefficiency")</h4>
    <hr />

    <div class="form-group">
        @SharedLocalizer.GetLocalizedHtmlString("Filtermaterial") :
        @Html.DisplayFor(model => model.filtermaterial.Name)
    </div>
    <div class="form-group">
        @SharedLocalizer.GetLocalizedHtmlString("Contamination") :
        @Html.DisplayFor(model => model.Contamination.Name)
    </div>
    <div class="form-group">
        @Html.DisplayNameFor(model => model.efficiency)
        @Html.DisplayFor(model => model.efficiency)
    </div>



    <form asp-action="Delete">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value=@SharedLocalizer.GetLocalizedHtmlString("Delete") class="btn btn-hoelscher" /> |
        <a asp-action="Index">@SharedLocalizer.GetLocalizedHtmlString("Back to List")</a>
    </form>
</div>
