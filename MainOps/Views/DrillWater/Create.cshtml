@model MainOps.Models.DrillWater

@{
    ViewData["Title"] = "Create";
}

<h2>Create</h2>

<h4>Handling of Drill Water</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create" method="post" enctype="multipart/form-data">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ProjectId" class="control-label"></label>
                <select asp-for="ProjectId" onchange="FillSubProjects();" class="form-control" asp-items="ViewBag.ProjectId"></select>
                <span asp-validation-for="ProjectId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="SubProjectId" class="control-label"></label>
                <select asp-for="SubProjectId" onchange="FillMeasPoints2();" class="form-control" asp-items="ViewBag.SubProjectId"></select>
                <span asp-validation-for="SubProjectId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MeasPointId" class="control-label"></label>
                <select asp-for="MeasPointId" class="form-control" asp-items="ViewBag.MeasPointId"></select>
            </div>
            <div class="form-group">
                <label asp-for="DrillWaterStart" class="control-label"></label>
                <input asp-for="DrillWaterStart" type="number" min="0" step="0.001" value="0" class="form-control" />
                <span asp-validation-for="DrillWaterStart" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DrillWaterEnd" class="control-label"></label>
                <input asp-for="DrillWaterEnd" type="number" min="0" step="0.001" value="0" class="form-control" />
                <span asp-validation-for="DrillWaterEnd" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AmountContainers" class="control-label"></label>
                <input asp-for="AmountContainers" type="number" min="0" step="1" value="1" class="form-control" />
                <span asp-validation-for="AmountContainers" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Photos" class="control-label"></label>
                <input type="file" multiple="multiple" name="photos" />
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts
    {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    <script type="text/javascript">


        function FillSubProjects() {
            var ProjectId = $('#ProjectId').val();
            var SubProjectId = $('#SubProjectId').val();
            var url = '@Url.Action("GetSubProjectList", "SubProjects")';
            $.ajax({
                type: 'GET',
                url: url,
                dataType: 'json',
                data: { 'theId': ProjectId },
                success: function (data) {
                    if (data.length > 0) {
                        var s = '<option value="">Please Select SubProject</option>';
                        for (var prop in data) {
                            s += '<option value="' + data[prop]["id"] + '">' + data[prop]["name"] + '</option>';
                        }
                        $("#SubProjectId").html(s);
                        document.getElementById("SubProjectId").style.width = "100%";
                        var x = document.getElementById("SubProjectId");
                        var i;
                        for (i = 0; i < x.length; i++) {
                            if (x.options[i].value == SubProjectId) {
                                x.options[i].selected = true;
                            }
                        }
                    }
                    else {
                        var s = '<option value="">Please Select SubProject</option>';
                        $("#SubProjectId").html(s);
                    }
                    FillMeasPoints();

                }
            });
        }
    function DisplayDocuments(index) {
            $.ajax({
                type: 'post',
                url: '@Url.Action("GetDocuments","MeasPoints")',
                data: {
                    'id': index
                },
                success: function (output) {
                    $('#documentinfo').html(output);
                },
                error: function () {
                    alert("error");
                }
            });
        }
    function FillMeasPoints() {
        var MeasPointId = $('#MeasPointId').val();
            var ProjectId = $('#ProjectId').val();
        var url = '@Url.Action("GetWellsProject", "Projects")';
            $.ajax({
                type: 'GET',
                url: url,
                dataType: 'json',
                data: {
                    'theId': ProjectId,
                },
                success: function (data) {
                    var s = '<option value="">Select Well</option>';
                    for (var prop in data) {
                        s += '<option value="' + data[prop]["id"] + '">' + data[prop]["name"] + '</option>';
                    }
                    $("#MeasPointId").html(s);
                    document.getElementById("MeasPointId").style.width = "100%";
                    var x = document.getElementById("MeasPointId");
                    var i;
                    for (i = 0; i < x.length; i++) {
                        if (x.options[i].value == MeasPointId) {
                            x.options[i].selected = true;
                        }
                    }
                }
            });
    }
    function FillMeasPoints2() {
        var MeasPointId = $("MeasPointId").val();
            var SubProjectId = $('#SubProjectId').val();
        var url = '@Url.Action("GetWellsSubProject", "SubProjects")';
            $.ajax({
                type: 'GET',
                url: url,
                dataType: 'json',
                data: {
                    'theId': SubProjectId,
                },
                success: function (data) {
                    var s = '<option value="">Select Well</option>';
                    for (var prop in data) {
                        s += '<option value="' + data[prop]["id"] + '">' + data[prop]["name"] + '</option>';
                    }
                    $("#MeasPointId").html(s);
                    document.getElementById("MeasPointId").style.width = "100%";
                    var x = document.getElementById("MeasPointId");
                    var i;
                    for (i = 0; i < x.length; i++) {
                        if (x.options[i].value == MeasPointId) {
                            x.options[i].selected = true;
                        }
                    }
                }
            });
        }
        $(document).ready(function () {
            FillSubProjects();
            });
    </script>
}